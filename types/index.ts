export type ButtonProps = {
  type: "button" | "submit";
  icon?: string;
  title: string;
  variant: string;
};

export type FeatureItemProps = {
  title: string;
  icon: string;
  description: string;
};
